import { Counter } from 'entities/Counter';
import React from 'react';

export default function MainPage() {
    return (
        <div className="page">
            MainPage
            <Counter />
        </div>
    );
}
